package com.example.Split_Wise_application.commands;

public interface CommandKeywords {

    //because of the interface we don't have to declare variable as public static final
    //to make the variable constants.
    public String REGISTER_USER_COMMAND = "Register";

    public String UPDATE_USER_COMMAND = "UpdateProfile";
}
